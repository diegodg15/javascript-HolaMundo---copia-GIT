/**Crear un algoritmo que devuelve el precio del producto más el impuesto*/

function precioCompleto(precio, impuesto) {
    precio + precio * impuesto;
}

let resutltado = precioCompleto(19.90, 0.15);
console.log(resultado);
