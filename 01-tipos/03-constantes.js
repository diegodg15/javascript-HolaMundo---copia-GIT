//Sin constante:
let nombre = "Ana";
nombre = "Felipe";
console.log(nombre);//imprime el ultimo valor asignado. Regla general: nunca cambiar los valores de las variables que tienen.

//Constante Const (variable de solo lectura, no se cambia)


